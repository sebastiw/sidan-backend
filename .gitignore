node_modules
.idea

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
config/prod.yaml

# Built binary
sidan-backend

# F-Droid signing keystore — never commit, losing it breaks existing repo installs
*.keystore

# Remove sidan-backups and locally uploaded files
static/*.png
sidan-backups/
hypea.png